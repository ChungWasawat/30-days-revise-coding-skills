apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
spec:
  clusterIP: None                      # ← makes it headless
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432